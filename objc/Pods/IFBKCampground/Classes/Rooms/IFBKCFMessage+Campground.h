#import "IFBKCFMessage.h"

/**
 * Category which provides helpers for Campground Messages.
 */
@interface IFBKCFMessage (Campground)

/**
 * Returns wether the message was generated by a user.
 */
- (BOOL)isUserGenerated;

/**
 * Returns wether the message represents a room event (e.g. a user joined the 
 * room).
 */
- (BOOL)isRoomEvent;

/**
 * Returns a human readable description of a room event suitable for UI.
 * This method raises if the given message is not a room event.
 *
 * @param userName The name of the user associated with the message, if any.
 */
- (NSString*)roomEventDescriptionWithUser:(NSString*)userName;

@end
